{
   "application": {
     "name": "TestApp",
     "version": "2.1.0",
     "environment": "staging"
  },
  "database": {
     "host": "localhost",
     "port": 5432,
     "name": "testdb",
     "credentials": {
       "username": "dbuser",
       "password": "dbpass123"
      }
  },
  "api": {
      "baseUrl": "https://api.staging.example.com",
      "timeout": 30000,
      "endpoints": {
        "login": "/auth/login",
        "users": "/api/v1/users"
       }
    }
  }